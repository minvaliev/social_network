import React from "react";
import classes from './Dialogs.module.css'

const Dialogs = (props) => {
    return (
        <div>
            Dialogs and message
        </div>
    );
}

export default Dialogs;